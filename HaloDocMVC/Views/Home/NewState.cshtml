@model HaloDocMVC.Entity.Models.PaginatedViewModel
@using static HaloDocMVC.Entity.Models.Constant

<div class="table-responsive m-2 mb-5 d-none d-sm-block">
    <table class="w-100 display dataTable">
        <thead class="theadbg py-2">
            <tr>
                @{
                    var i1 = @Model.SortedColumn;
                    var J1 = @Model.IsAscending;
                    <th scope="col" style="position:relative;" class="dt-orderable-asc dt-orderable-desc @(i1 == "PatientName" ? (J1 == true ?"dt-ordering-asc" : "dt-ordering-desc" ) : "")" onclick="SetColumValue('PatientName')">
                        <small>Name</small>
                        <span class="dt-column-order"></span>
                    </th>
                    <th></th>
                    @if (CredentialValue.role() == "Admin")
                    {
                        <th scope="col" style="position:relative;" class="dt-orderable-asc dt-orderable-desc @(i1 == "DateOfBirth" ? (J1 == true ?"dt-ordering-asc" : "dt-ordering-desc" ) : "")" onclick="SetColumValue('DateOfBirth')">
                            <small>Date Of Birth</small>
                            <span class="dt-column-order"></span>
                        </th>
                        <th scope="col" style="position:relative;" class="dt-orderable-asc dt-orderable-desc @(i1 == "Requestor" ? (J1 == true ?"dt-ordering-asc" : "dt-ordering-desc" ) : "")" onclick="SetColumValue('Requestor')">
                            <small>Requestor</small>
                            <span class="dt-column-order"></span>
                        </th>
                        <th scope="col" style="position:relative;" class="dt-orderable-asc dt-orderable-desc @(i1 == "RequestedDate" ? (J1 == true ?"dt-ordering-asc" : "dt-ordering-desc" ) : "")" onclick="SetColumValue('RequestedDate')">
                            <small>Requested Date</small>
                            <span class="dt-column-order"></span>
                        </th>
                    }
                    <th scope="col">
                        <small>Phone</small>
                    </th>
                    <th scope="col" style="position:relative;" class="dt-orderable-asc dt-orderable-desc @(i1 == "Address" ? (J1 == true ?"dt-ordering-asc" : "dt-ordering-desc" ) : "")" onclick="SetColumValue('Address')">
                        <small>Address</small>
                        <span class="dt-column-order"></span>
                    </th>
                    @if (CredentialValue.role() == "Admin")
                    {
                        <th scope="col">
                            <small>Notes</small>
                        </th>
                        <th scope="col">
                            <small>Chat With</small>
                        </th>
                    }
                    <th scope="col">
                        <small>Actions</small>
                    </th>
                }
            </tr>
        </thead>
        <tbody class="mb-5">
            @foreach (var (item, index) in Model.adl.Select((item, index) => (item, index)))
            {
                <tr class="@Enum.GetName(typeof(ListType), item.RequestTypeId) border-bottom border-white text-white">
                    <td><small>@item.PatientName</small></td>
                    <td>
                        <button class="btn border-white text-white">
                            <i class="fa-regular fa-envelope"></i>
                        </button>
                    </td>
                    @if (CredentialValue.role() == "Admin")
                    {
                        <td>
                            <small>@item.DateOfBirth.ToString()</small>
                        </td>
                        <td><small>@item.Requestor</small></td>
                        <td><small>@item.RequestedDate.ToString("MMM d , yyyy")</small></td>
                    }
                    <td>
                        <button class="btn border border-white text-white mt-2">
                            <i class="fa-solid fa-phone pe-2"></i>
                            <small>@item.PatientPhoneNumber</small>
                        </button>
                        <div>
                            <small>(Patient)</small>
                        </div>

                        @if (item.RequestTypeId != 2)
                        {
                            <button class="btn border border-white text-white mt-2">
                                <i class="fa-solid fa-phone pe-2"></i>
                                <small>@item.RequestorPhoneNumber</small>
                            </button>
                            <div>
                                <small>
                                    (@Enum.GetName(typeof(ListType), item.RequestTypeId))
                                </small>
                            </div>
                        }
                    </td>
                    <td><small>@item.Address</small></td>
                    @if (CredentialValue.role() == "Admin")
                    {
                        <td><small>@item.Notes</small></td>
                        <td>
                            <button class="btn border border-white text-white my-2">
                                <small>Provider</small>
                            </button>
                        </td>
                    }
                    <td>
                        <button class="btn border border-white text-white my-2" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <small>Actions</small>
                        </button>
                        <ul class="dropdown-menu">
                            @if (CredentialValue.role() == "Admin")
                            {
                                <li><a class="dropdown-item" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#assigncase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName"><i class="fa-solid fa-list"></i> Assign Case</a></li>
                                <li><a class="dropdown-item" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#cancelcase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName"><i class="fa-regular fa-circle-xmark"></i> Cancel Case</a></li>
                            }
                            @if (CredentialValue.role() == "Provider")
                            {
                                <li><a class="dropdown-item" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#acceptcase" data-bs-requestid="@item.RequestId"><i class="fa-solid fa-ban"></i> Accept</a></li>
                            }
                            <li><a asp-controller="Home" asp-action="ViewCase" asp-route-RId="@item.RequestId" asp-route-RTId="@item.RequestTypeId" asp-route-Status="1" class="dropdown-item"><i class="fa-regular fa-rectangle-list"></i> View Case</a></li>
                            <li><a asp-controller="Home" asp-action="ViewNotes" asp-route-RId="@item.RequestId" class="dropdown-item"><i class="fa-regular fa-note-sticky"></i> View Notes</a></li>
                            @if (CredentialValue.role() == "Admin")
                            {
                                <li><a class="dropdown-item" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#blockcase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName"><i class="fa-solid fa-ban"></i> Block Patient</a></li>
                            }
                        </ul>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div class="accordion mt-3 d-block d-sm-none" id="accordionExample">
    @foreach (var (item, index) in Model.adl.Select((item, index) => (item, index)))
    {
        <div class="accordion-item s7">
            <div class="accordion-header" id="flush-heading-@item.RequestId">
                <div class="container-fluid" type="button" data-bs-toggle="collapse" data-bs-target="#flush-@item.RequestId" aria-expanded="true" aria-controls="collapse_@item.RequestId">
                    <div class="row w-100">
                        <div class="col-6 mb-3">
                            <small>@item.PatientName</small>
                        </div>
                        <div class="col-6 mb-3 text-end">
                            <small>(@Enum.GetName(typeof(ListType), item.RequestTypeId))</small>
                            <i class="fa-solid fa-circle txt-@Enum.GetName(typeof(ListType),item.RequestTypeId)"></i>
                        </div>
                        <div class="col-6">
                            @item.Address
                        </div>
                        <div class="col-6 text-end">
                            <button class="btn btn-sm btn-white border-info text-info rounded-pill 2px">
                                <small>Map Location</small>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div id="flush-@item.RequestId" class="accordion-collapse collapse mb-3 border-bottom" data-bs-parent="#accordionExample">
                <div class="accordion-body container-fluid">
                    <div class="row mb-2">
                        <div class="col-10 horRule">
                            <hr>
                        </div>
                        <div class="col-2 viewCaseMobile">
                            <a asp-controller="Home" asp-action="ViewCase" asp-route-RId="@item.RequestId" asp-route-RTId="@item.RequestTypeId" asp-route-Status="1" class="btn btn-sm rounded-pill 2px" style="border-color: #A020F0; color: #A020F0">
                                <small>View Case</small>
                            </a>
                        </div>
                    </div>
                    @if (CredentialValue.role() == "Admin")
                    {
                        <p><i class="btn btn-outline-info rounded-pill fa-solid fa-calendar-days me-2"></i>Date of Birth: @item.DateOfBirth.ToString()</p>
                        <p><i class="btn btn-outline-info rounded-pill fa-solid fa-envelope me-2"></i>Email : @item.Email</p>
                    }
                    <p><i class="btn btn-outline-info rounded-pill fa-solid fa-phone me-2"></i>Patient : @item.PatientPhoneNumber</p>
                    <p><i class="btn btn-outline-info rounded-pill fa-solid fa-user me-2"></i>Requestor : @item.Requestor</p>
                    <div class="row">
                        @if (CredentialValue.role() == "Admin")
                        {
                            <div class="col-6 mb-3">
                                <a class="btn btn-white text-white rounded-pill 2px w-100" style="background-color: #A020F0;" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#assigncase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName">
                                    <small>Assign Case</small>
                                </a>
                            </div>
                            <div class="col-6 mb-3">
                                <button class="btn btn-white bg-danger text-white rounded-pill 2px w-100" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#cancelcase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName">
                                    <small>Cancel Case</small>
                                </button>
                            </div>
                        }
                        <div class="col-6 mb-3">
                            <a asp-controller="Home" asp-action="ViewNotes" asp-route-RId="@item.RequestId" class="btn btn-white bg-success text-white rounded-pill 2px w-100">
                                <small>View Notes</small>
                            </a>
                        </div>
                        @if (CredentialValue.role() == "Admin")
                        {
                            <div class="col-6 mb-3">
                                <button class="btn btn-white bg-danger text-white rounded-pill 2px w-100" onclick="model_Case(@index)" data-bs-toggle="modal" data-bs-target="#blockcase" data-bs-requestid="@item.RequestId" data-bs-patientname="@item.PatientName">
                                    <small>Block Patient</small>
                                </button>
                            </div>
                            <div class="col-6 mb-3 ">
                                <button class="btn btn-white bg-success text-white rounded-pill 2px w-100">
                                    <small>Email</small>
                                </button>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>
<div class="m-2 d-flex justify-content-end">
    <nav aria-label="Page navigation example">
        <ul class="pagination pagination-container">
            @{
                int i;
                <form id="paginationform" data-ajax="true" data-ajax-method="post" data-ajax-mode="replace" data-ajax-update="#SearchResult"
                      asp-action="_SearchResult" asp-controller="Home" novalidate="novalidate" class="mt-0 d-flex">
                    <input type="hidden" asp-for="RegionId" />
                    <input type="hidden" asp-for="RequestType" />
                    <input type="hidden" asp-for="SearchInput" />
                    <input type="hidden" asp-for="PageSize" />
                    <input type="hidden" asp-for="CurrentPage" id="currentpagevalue" />
                    <input type="hidden" asp-for="SortedColumn" />
                    <input type="hidden" asp-for="IsAscending" />
                    @if (@Model.CurrentPage > 1)
                    {
                    <li class="page-item me-2"><button class="btn shadow-none" onclick="setpagenumber('previous')">&lt;</button></li>
                    }
                    else
                    {
                    <li class="me-2 page-item"><a class="disabled btn">&lt;</a></li>
                    }
                    @for (i = 1; i <= @Model.TotalPages; i++)
                    {
                    <li class="page-item me-2"><button class="@(i == @Model.CurrentPage ? "activepagebtn" : "") btn shadow-none" onclick="setpagenumber(@i)">@i</button></li>
                    }
                    @if (@Model.TotalPages > @Model.CurrentPage)
                    {
                    <li class="page-item me-2"><button class="btn shadow-none" onclick="setpagenumber('next')">&gt;</button></li>
                    }
                    else
                    {
                    <li class="me-2 page-item"><a class="btn disabled">&gt;</a></li>
                    }
                </form>
            }
        </ul>
    </nav>
</div>
<form class="sortingform" data-ajax="true" data-ajax-method="post" data-ajax-mode="replace" data-ajax-update="#SearchResult"
      asp-action="_SearchResult" asp-controller="Home" novalidate="novalidate">
    <input type="hidden" asp-for="RegionId" />
    <input type="hidden" asp-for="RequestType" />
    <input type="hidden" asp-for="SearchInput" />
    <input type="hidden" asp-for="SortedColumn" id="sortedcol" />
    <input type="hidden" asp-for="IsAscending" id="isAsc" />
</form>
<script>
    function setpagenumber(e) {
        if (e == 'previous') {
            var cp = @Model.CurrentPage-1;
            $('#currentpagevalue').val(cp);
        }
        else if (e == 'next') {
            var cp = @Model.CurrentPage + 1;
            $('#currentpagevalue').val(cp);
        }
        else {
            $('#currentpagevalue').val(e);
        }
        $("#paginationform").submit();
    }
    function SetColumValue(e) {
        var modeldata = @Html.Raw(Json.Serialize(Model));
        console.log(modeldata)
        if (modeldata.sortedColumn === e) {
            if (modeldata.isAscending == true) {
                document.getElementById('isAsc').value = false;
            }
            else {
                document.getElementById('isAsc').value = true;
            }
            console.log(document.getElementById('isAsc').value);
        }
        else {
            document.getElementById('sortedcol').value = e;
            document.getElementById('isAsc').value = true;
        }
        $(".sortingform").submit();
    }
</script>
<script>
    function model_Case(id) {
        var modeldata = @Html.Raw(Json.Serialize(Model.adl))
        document.getElementById('RequestId_Input_ModelAssignCase').value = modeldata[id].requestId;
        document.getElementById('PatientName_ModelCancelCase').innerText = modeldata[id].patientName;
        document.getElementById('RequestID_Input_ModelCancelCase').value = modeldata[id].requestId;
        document.getElementById('RequestID_Input_ModelBlockCase').value = modeldata[id].requestId;
        document.getElementById('RequestID_Input_ModelAcceptCase').value = modeldata[id].requestId;
        document.getElementById('PatientName_ModelBlockCase').innerText = modeldata[id].patientName;
    }
</script>